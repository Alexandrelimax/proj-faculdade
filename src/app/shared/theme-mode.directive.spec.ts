import { ThemeModeDirective } from './theme-mode.directive';

describe('ThemeModeDirective', () => {
  it('should create an instance', () => {
    const directive = new ThemeModeDirective();
    expect(directive).toBeTruthy();
  });
});
